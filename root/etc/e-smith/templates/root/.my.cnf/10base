[client]
{
    #----------------------------------------------------------
    # Read (and possibly create) mysql password
    #----------------------------------------------------------

    use esmith::util;
    my $pw = esmith::util::genRandomPassword('/etc/my.pwd');
    $OUT .= "password=$pw";
}
